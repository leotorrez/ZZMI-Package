namespace = ZZMI

[CommandListSaveWorldDefault]
if DRAW_TYPE == 2 || DRAW_TYPE == 4
	run = CommandList\SlotFix\SaveWorldDefault
endif

[CommandListSetWorldTextures]
if DRAW_TYPE == 2 || DRAW_TYPE == 4
	run = CommandList\SlotFix\SlotFixWorldUnlessNull
endif
run = CommandList.Clear.Main

[CommandListLoadWorldDefault]
run = CommandList.Set.Default
run = CommandListSetWorldTextures
run = CommandList.Clear.Default
